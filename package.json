{
  "devDependencies": {
    "serverless": "^4.3.2",
    "serverless-python-requirements": "^6.1.1"
  },
  "scripts": {
    "deploy": "serverless deploy",
    "go": "serverless invoke local --function cron"
  }
}
